#include "sync.hpp"
#include <iostream>
#include <string>

void runSync(const std::string& config_file) {
    std::cout << "Running sync with config: "
              << config_file
              << std::endl;
}
