#include "sync.hpp"
#include <iostream>
#include <string>
#include <yaml-cpp/yaml.h>

int run_sync(const std::string& config_file) {
    std::cout << "Running sync with config: "
              << config_file
              << std::endl;
    return 0;
}
