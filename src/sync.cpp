#include "sync.hpp"
#include <iostream>
#include <string>

int runSync(const std::string& config_file) {
    std::cout << "Running sync with config: "
              << config_file
              << std::endl;
    return 0;
}
